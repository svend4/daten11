# Деплой на Render.com

## Введение

Это руководство поможет вам развернуть систему метаданных на Render.com для тестирования и демонстрации.

## Что будет развернуто

- ✅ Flask веб-приложение
- ✅ REST API для работы с метаданными
- ✅ Демонстрационная библиотека (examples/sample_library)
- ✅ Поиск по метаданным
- ✅ Веб-интерфейс для навигации

## Предварительные требования

1. Аккаунт на [Render.com](https://render.com) (бесплатный план)
2. GitHub аккаунт (для подключения репозитория)
3. Ваш репозиторий с проектом daten11

## Способ 1: Автоматический деплой через render.yaml (Рекомендуется)

### Шаг 1: Подготовка репозитория

Все необходимые файлы уже созданы:
- ✅ `render.yaml` - конфигурация Render
- ✅ `requirements.txt` - зависимости Python
- ✅ `web/app.py` - Flask приложение
- ✅ `web/templates/index.html` - веб-интерфейс

### Шаг 2: Создание сервиса на Render

1. **Войдите на Render.com**
   - Перейдите на https://render.com
   - Войдите через GitHub

2. **Создайте новый Blueprint**
   - Нажмите "New +" → "Blueprint"
   - Подключите ваш GitHub репозиторий
   - Выберите репозиторий `daten11`
   - Выберите ветку `claude/add-file-metadata-support-93MCI`

3. **Render автоматически обнаружит render.yaml**
   - Нажмите "Apply"
   - Render создаст веб-сервис автоматически

4. **Дождитесь деплоя**
   - Процесс займет 3-5 минут
   - Вы увидите логи сборки в реальном времени

5. **Получите URL**
   - После деплоя вы получите URL вида:
   - `https://metadata-system.onrender.com`

### Шаг 3: Проверка работы

Откройте URL в браузере и проверьте:
- ✅ Главная страница загружается
- ✅ Поиск работает
- ✅ Можно просматривать демо-документы
- ✅ API отвечает на `/api/stats`

## Способ 2: Ручной деплой через Dashboard

Если автоматический деплой не работает, используйте ручной способ:

### Шаг 1: Создайте Web Service

1. На Render Dashboard нажмите "New +" → "Web Service"
2. Подключите GitHub репозиторий
3. Выберите репозиторий `daten11`

### Шаг 2: Настройте сервис

**Name:** `metadata-system` (или любое другое)

**Region:** Frankfurt (EU Central) или ближайший к вам

**Branch:** `claude/add-file-metadata-support-93MCI`

**Runtime:** `Python 3`

**Build Command:**
```bash
pip install -r requirements.txt
```

**Start Command:**
```bash
gunicorn --chdir web app:app
```

**Plan:** Free

### Шаг 3: Переменные окружения

Добавьте переменные окружения:

| Key | Value |
|-----|-------|
| `PYTHON_VERSION` | `3.11.0` |
| `DOCUMENTS_PATH` | `/opt/render/project/src/examples/sample_library` |

### Шаг 4: Создайте сервис

Нажмите "Create Web Service" и дождитесь деплоя.

## Способ 3: Деплой через CLI

### Установите Render CLI

```bash
# Установите Node.js если еще нет
# Затем установите Render CLI
npm install -g @render/cli

# Авторизуйтесь
render login
```

### Создайте сервис

```bash
# Перейдите в директорию проекта
cd /path/to/daten11

# Создайте сервис
render blueprint create

# Следуйте инструкциям
```

## Тестирование развернутого приложения

### Базовые проверки

```bash
# Health check
curl https://your-app.onrender.com/health

# Статистика
curl https://your-app.onrender.com/api/stats

# Фасеты
curl https://your-app.onrender.com/api/facets

# Поиск
curl "https://your-app.onrender.com/api/search?q=квантовая"
```

### Веб-интерфейс

Откройте в браузере:
```
https://your-app.onrender.com
```

Возможности:
- 🔍 Поиск по документам
- 📁 Просмотр папок
- 📄 Просмотр метаданных файлов
- 📊 Статистика коллекции
- 🏷️ Фильтрация по тегам и категориям

## Загрузка собственных документов

### Вариант 1: Через Git (для постоянного хранения)

```bash
# Локально добавьте документы
cd /path/to/daten11/examples/sample_library

# Создайте новую папку
mkdir my_documents

# Добавьте файлы
cp /path/to/your/docs/* my_documents/

# Создайте метаданные
python3 ../../tools/metadata_manager.py init-folder my_documents \
  --name "Мои документы" \
  --description "Тестовая коллекция"

# Создайте метаданные для файлов
../../scripts/auto_metadata.sh my_documents --files-only

# Закоммитьте
git add examples/sample_library/my_documents
git commit -m "Добавлены тестовые документы"
git push

# Render автоматически задеплоит обновления
```

### Вариант 2: Через Render Disk (платный план)

На бесплатном плане файловая система эфемерная (сбрасывается при перезапуске).
Для постоянного хранения нужен платный plan с Disk.

## Добавление Meilisearch (опционально)

### Шаг 1: Создайте Private Service для Meilisearch

1. В Render Dashboard: "New +" → "Private Service"
2. Выберите "Docker"
3. **Image URL:** `getmeili/meilisearch:v1.5`
4. **Plan:** Starter ($7/мес)
5. **Disk:** 1GB для хранения индекса

### Шаг 2: Получите internal URL

Render создаст internal URL вида:
```
meilisearch:7700
```

### Шаг 3: Обновите веб-сервис

Добавьте переменную окружения:

| Key | Value |
|-----|-------|
| `MEILISEARCH_URL` | `http://meilisearch:7700` |

### Шаг 4: Обновите код

В `web/app.py` добавьте инициализацию Meilisearch:

```python
MEILISEARCH_URL = os.getenv('MEILISEARCH_URL')
if MEILISEARCH_URL:
    from meilisearch_indexer import MeilisearchIndexer
    meilisearch = MeilisearchIndexer(host=MEILISEARCH_URL)
```

## Мониторинг и логи

### Просмотр логов

1. Откройте Dashboard сервиса
2. Вкладка "Logs"
3. Смотрите логи в реальном времени

### Метрики

- CPU usage
- Memory usage
- Request count
- Response time

Доступны на вкладке "Metrics"

## Оптимизация для Render

### 1. Кеширование зависимостей

Render автоматически кеширует pip пакеты между деплоями.

### 2. Уменьшение времени билда

Минимизируйте зависимости в `requirements.txt`:

```bash
# Установите только необходимое
Flask==3.0.0
gunicorn==21.2.0
```

### 3. Health checks

Приложение уже содержит эндпоинт `/health`:

```python
@app.route('/health')
def health():
    return jsonify({'status': 'healthy'}), 200
```

### 4. Graceful shutdown

Gunicorn автоматически обрабатывает graceful shutdown.

## Ограничения бесплатного плана

⚠️ **Важно знать:**

- ❌ Сервис "засыпает" после 15 минут неактивности
- ❌ Первый запрос после сна занимает 30-60 секунд
- ❌ 750 часов/месяц бесплатного времени работы
- ❌ Файловая система эфемерная (не сохраняется между деплоями)
- ✅ Достаточно для демо и тестирования

## Обновление приложения

### Автоматическое

При включенном Auto-Deploy:
```bash
git push origin claude/add-file-metadata-support-93MCI
# Render автоматически задеплоит
```

### Ручное

1. Dashboard → ваш сервис
2. Нажмите "Manual Deploy"
3. Выберите "Clear build cache & deploy" если нужно

## Переход на платный план

Для production использования рекомендуется:

**Starter Plan ($7/мес):**
- Постоянная работа (без "сна")
- Больше ресурсов
- Persistent disk

**Pro Plan ($25/мес):**
- Еще больше ресурсов
- Автоскейлинг
- Priority support

## Альтернативы Render.com

Если Render не подходит, рассмотрите:

### 1. Railway.app
```bash
# Аналогичный Render
# Также поддерживает render.yaml
```

### 2. Fly.io
```bash
# Больше контроля
# Требует Dockerfile
```

### 3. Google Cloud Run
```bash
# Serverless
# Оплата за использование
```

### 4. Heroku
```bash
# Классический PaaS
# Procfile вместо render.yaml
```

## Демо-примеры

### Публичные демо на Render

Посмотрите рабочие примеры:
```
https://metadata-system-demo.onrender.com
```

(Если вы развернули и хотите поделиться, добавьте URL здесь)

## Устранение проблем

### Проблема: Build failed

**Решение:**
```bash
# Проверьте requirements.txt
# Убедитесь что все зависимости совместимы
pip install -r requirements.txt  # локально
```

### Проблема: Application error

**Решение:**
```bash
# Проверьте логи в Dashboard
# Обычно проблема в импортах или путях

# Проверьте что DOCUMENTS_PATH правильный
echo $DOCUMENTS_PATH
```

### Проблема: Медленная работа

**Решение:**
- Это нормально для free plan
- Upgrade до Starter для постоянной работы

### Проблема: Out of memory

**Решение:**
```bash
# Уменьшите количество workers Gunicorn
gunicorn --workers 1 --chdir web app:app

# Или в render.yaml:
# startCommand: gunicorn --workers 1 --threads 2 --chdir web app:app
```

## Следующие шаги

После успешного деплоя:

1. ✅ **Протестируйте все функции**
2. ✅ **Добавьте свои документы**
3. ✅ **Настройте домен (custom domain)**
4. ✅ **Добавьте Meilisearch для быстрого поиска**
5. ✅ **Настройте мониторинг**
6. ✅ **Перейдите на платный план для production**

## Поддержка

Если возникли проблемы:
- 📖 [Render Documentation](https://render.com/docs)
- 💬 [Render Community](https://community.render.com)
- 🐛 [GitHub Issues](https://github.com/svend4/daten11/issues)

## Заключение

Теперь у вас есть публично доступная демонстрация системы метаданных!

Поделитесь URL с коллегами и протестируйте все возможности онлайн.
